import type{Metadata}from"next";import{notFound}from"next/navigation";import{ServicePage}from"@/components/service-page";import{services}from"@/lib/site-data";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,s=services.find(x=>x.slug===slug);if(!s)return{};const t=s.text.en;return{title:t.title,description:t.summary,alternates:{canonical:`/services/${slug}`,languages:{"en-AU":`/services/${slug}`,es:`/es/services/${slug}`,fr:`/fr/services/${slug}`}}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,s=services.find(x=>x.slug===slug);if(!s)notFound();return <ServicePage service={s} lang="en"/>}
