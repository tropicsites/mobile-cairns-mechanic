import type{Metadata}from"next";import{notFound}from"next/navigation";import{ServicePage}from"@/components/service-page";import{Lang,services}from"@/lib/site-data";
export function generateStaticParams(){return ["es","fr"].flatMap(lang=>services.map(s=>({lang,slug:s.slug})))}export async function generateMetadata({params}:{params:Promise<{lang:string;slug:string}>}):Promise<Metadata>{const{lang,slug}=await params,s=services.find(x=>x.slug===slug);if(!s||!['es','fr'].includes(lang))return{};const t=s.text[lang as Lang];return{title:t.title,description:t.summary,alternates:{canonical:`/${lang}/services/${slug}`,languages:{"en-AU":`/services/${slug}`,es:`/es/services/${slug}`,fr:`/fr/services/${slug}`}}}}export default async function Page({params}:{params:Promise<{lang:string;slug:string}>}){const{lang,slug}=await params,s=services.find(x=>x.slug===slug);if(!s||!['es','fr'].includes(lang))notFound();return <ServicePage service={s} lang={lang as Lang}/>}
